
```c++
#include <stdio.h>
#include <Windows.h>

typedef Person* (*GetPersonInstance)();
typedef void ReleaseInstance(Person *p);

int main()
{
   HMODULE hModule = LoadLibrary("CompanyA.dll");
   if(NULL == hModule){
      printf("加载失败\n");
      return 0;
   }
   GetProcAddress(hModule, "?GetPersonInstance@@YAPAVPerson@@XZ");
   getchar();
   
   return 0;
}
```
